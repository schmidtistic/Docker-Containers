services:
  traefik:
    image: traefik:v2.11
    container_name: traefik
    restart: unless-stopped

    command:
      - "--api.dashboard=true"
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--log.level=INFO"

    ports:
      - "127.0.0.1:18080:8080"
      - "8081:80"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${APPDATA_ROOT}/traefik:/traefik

    networks:
      - SchmidtHouse

    labels:
      - "net.unraid.docker.icon=https://raw.githubusercontent.com/selfhst/icons/main/png/traefik.png"

networks:
  SchmidtHouse:
    external: true
